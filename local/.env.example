# Copy to local/.env (gitignored) and fill in. Never commit local/.env.
CLOUD_URL=https://aprilgatehq.com
INGEST_KEY=change-me-to-match-the-ingest-secret
# devnet RPC for the /solana relay (plain devnet, helius, quicknode, …). Required.
SOLANA_RPC_URL=https://api.devnet.solana.com
PORT=8080
