/tmp
/public/assets/aprilgate.js
/out-tsc
vcs.xml
modules.xml
/node_modules
/.wrangler
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js
/.idea
.vscode/*
.env
.env.*
!local/.env.example
local/.env
.DS_Store
.dev.vars
*.zip


# Source video masters. The web-ready transcode lives in public/assets/ and IS
# committed; the full-size originals are not (they also exceed Cloudflare Pages'
# 25 MiB per-file limit, so they can never be served — keeping one under public/
# would fail the next deploy outright).
/*.mov
/*.MOV
/media/
/CLAUDE_INSTRUCTIONS

